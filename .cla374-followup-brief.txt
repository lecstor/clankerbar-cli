SCOPED FOLLOW-UP REVIEW. A full adversarial pass already ran on this change; its findings were fixed. Your job: verify each fix and hunt for regressions the fixes introduced - within the surfaces they touched ONLY. Do not re-review the whole change.

Repo worktree: /Users/jason/dev/clankerbar-cli-wt/3adf35f6 (Go repo github.com/lecstor/clankerbar-cli, branch clanker/3adf35f6-a-killed-session-s-grandchildren-survive, merge-commit tip e171655).

The FIX DIFF (uncommitted, on top of e171655):
git -C /Users/jason/dev/clankerbar-cli-wt/3adf35f6 diff e171655 -- internal/harness/

The PRIOR FULL REVIEW (its findings, verbatim): /Users/jason/dev/clankerbar-cli-wt/3adf35f6/.cla374-review-result.txt

The findings claimed FIXED (by name):
1. [HIGH] opencode.go monitor goroutine racing on cmd.Process (CI-breaking under go test -race) - replaced with exec.Cmd.Cancel in both adapters (claude.go, opencode.go).
2. [MEDIUM] group kill did not fire on plain caller cancellation (Ctrl-C no longer reaching the session subtree once Setpgid moved it out of the foreground group) - Cancel fires on ANY sctx cancellation now.
3. [MEDIUM] opencode group kill firing after Run reaped the leader (kill against possibly-recycled pid) - os/exec calls Cancel only while the process is live.
4. [LOW] comments in opencode.go and wallclock_test.go asserting the orphan is deliberately NOT killed - rewritten: WaitDelay is the backstop BEHIND the group kill.
5. [LOW] processgroup_test.go: the elapsed bound was checked only after Invoke returned (not a bound), and comments blamed the stdout pipe when the stderr MultiWriter is what blocks Wait - Invoke now runs on its own goroutine behind a select deadline (invokeWithBound); comments corrected.
6. [LOW] SIGKILL-without-grace was an unstated choice - now stated deliberate in killProcessGroup's comment.

The finding deliberately NOT FIXED (check the mitigation is honest):
7. [MEDIUM] claude's session path sets no cmd.WaitDelay, so an out-of-group escapee (setsid) could still block Wait - mitigated by a comment naming the gap ("Known gap, deliberate"); a follow-up task is being filed.

Regression surface (restrict your attack to these): internal/harness/claude.go Invoke/consume cancellation paths; internal/harness/opencode.go runSession cap/cancellation paths; internal/harness/processgroup_unix.go and processgroup_other.go; internal/harness/processgroup_test.go and wallclock_test.go.

Verify claims by running, from the CURRENT tree only:
cd /Users/jason/dev/clankerbar-cli-wt/3adf35f6 && go test ./internal/harness/ -race -count=1   (and -run filters of it)

HARD RULES: do NOT modify repository state. Never checkout, switch, reset, stash, clean, restore, merge, rebase, commit, push, or git worktree add/remove. If a claim seems to need another branch's behavior, reason from the diff and the code instead. These are also blocked by your tool permissions.

Report: for each numbered finding, VERIFIED-FIXED / NOT-FIXED / REGRESSION (with file, line, what). Then any NEW findings in the regression surface, severity-tagged ([ERROR]/[WARN]/[NIT]), file and line, what is wrong, what would fix it. Say plainly where you found nothing. Distinguish verified from suspected. Your final message IS the review.
